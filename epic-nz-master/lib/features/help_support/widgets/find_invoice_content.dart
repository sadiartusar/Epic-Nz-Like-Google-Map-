import 'package:flutter/material.dart';

class FindInvoiceContent extends StatelessWidget {
  const FindInvoiceContent({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text(
      "Go to settings & tap on invoice & download pdf .",
      style: TextStyle(color: Colors.grey, fontSize: 13, height: 1.5),
    );
  }
}